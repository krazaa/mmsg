<x-app-layout>
<div class="py-5 sm:py-8"><div class="mx-auto max-w-7xl space-y-5 px-3 sm:px-4">
<section class="admin-command-card admin-command-summary p-5 text-white shadow-2xl sm:p-8">
    <div class="absolute -right-16 -top-20 h-64 w-64 rounded-full"></div>
    <div class="absolute -bottom-24 left-1/3 h-56 w-56 rounded-full "></div>
    <div class="relative flex flex-col gap-5 sm:flex-row sm:items-end sm:justify-between">
        <div><div class="admin-command-badge inline-flex rounded-lg px-3 py-1.5 text-[10px] font-black uppercase tracking-[.22em]">Booking management</div><h1 class="mt-2 text-3xl font-black tracking-tight sm:text-4xl">Bookings</h1><p class="mt-2 max-w-xl text-sm leading-6 text-indigo-100/80">Review property requests, monitor active plans and manage customer bookings.</p></div>
        <a href="{{ route('sales.create') }}" class="admin-command-action inline-flex w-full items-center justify-center gap-2 rounded-2xl px-5 py-3 text-sm font-black shadow-xl shadow-black/20 transition hover:-translate-y-0.5 hover:brightness-90 sm:w-auto"><span class="text-lg leading-none">+</span> New booking</a>
    </div>
    <div class="admin-command-stat-grid mt-4 grid grid-cols-2 gap-2.5 lg:grid-cols-4">
        @foreach([
            ['All bookings', $summary['total'], 'Total booking records', 'bg-slate-400', false],
            ['Pending review', $summary['pending'], 'Awaiting decision', 'bg-amber-400', false],
            ['Active plans', $summary['active'], 'Currently active', 'bg-emerald-400', false],
            ['Amount received', $summary['received'], 'Verified payments', 'bg-cyan-400', true],
        ] as [$label, $value, $hint, $accent, $currency])
            <div class="admin-command-stat rounded-2xl border p-3.5 backdrop-blur sm:p-4">
                <div class="flex items-center gap-2 text-[9px] font-black uppercase tracking-wider text-indigo-200 sm:text-[10px]"><span class="h-2 w-2 shrink-0 rounded-full {{ $accent }}"></span>{{ $label }}</div>
                <div class="mt-2 break-words text-xl font-black tabular-nums text-white sm:text-3xl">{{ $currency ? 'Rs '.number_format($value) : number_format($value) }}</div>
                <div class="mt-1 text-[10px] leading-4 text-indigo-100/65 sm:text-xs">{{ $hint }}</div>
            </div>
        @endforeach
    </div>
</section>
@if(session('success'))<div class="rounded-lg bg-green-100 p-4 text-green-800">{{ session('success') }}</div>@endif @if($errors->any())<div class="rounded-lg bg-red-100 p-4 text-red-800">{{ $errors->first() }}</div>@endif
<form method="GET" class="grid gap-4 rounded-xl bg-white p-4 shadow sm:grid-cols-2 sm:p-5 lg:grid-cols-4 dark:bg-slate-800 dark:text-slate-100"><label class="text-sm font-medium">Search<input name="search" value="{{ request('search') }}" placeholder="Booking, customer, CNIC or phone" class="mt-1 w-full rounded-md border-gray-300 dark:border-slate-600 dark:bg-slate-900"></label><label class="text-sm font-medium">Project<select name="project" class="mt-1 w-full rounded-md border-gray-300 dark:border-slate-600 dark:bg-slate-900"><option value="">All projects</option>@foreach($projects as $project)<option value="{{ $project->id }}" @selected(request('project')==$project->id)>{{ $project->name }}</option>@endforeach</select></label><label class="text-sm font-medium">Status<select name="status" class="mt-1 w-full rounded-md border-gray-300 dark:border-slate-600 dark:bg-slate-900"><option value="">All statuses</option>@foreach(['pending','approved','active','completed','cancelled','defaulted'] as $status)<option value="{{ $status }}" @selected(request('status')===$status)>{{ ucfirst($status) }}</option>@endforeach</select></label><div class="grid grid-cols-2 items-end gap-2"><button class="rounded-md bg-gray-800 px-5 py-2.5 font-semibold text-white">Filter</button><a href="{{ route('bookings.index') }}" class="rounded-md border px-4 py-2.5 text-center dark:border-slate-600">Clear</a></div></form>
<div class="rounded-xl bg-white shadow dark:bg-slate-800"><table class="admin-bookings-table w-full text-sm"><thead class="bg-gray-50 text-left text-gray-500 dark:bg-slate-900 dark:text-slate-300"><tr><th class="p-4">Booking</th><th>Customer</th><th>Project</th><th>Package</th><th>Total</th><th>Received</th><th>Referrer</th><th>Status</th><th class="pe-4 text-right">Actions</th></tr></thead><tbody>
@forelse($bookings as $booking)
<tr class="border-t dark:border-slate-700 dark:text-slate-100" x-data="{ open:false, decision:'', notes:'', addNote(note){const current=(this.notes||'').trim();this.notes=current?current+'\n'+note:note} }">
    <td data-label="Booking" class="p-4"><span><b>{{ $booking->booking_number }}</b><small class="block text-xs text-gray-500 dark:text-slate-400">{{ $booking->booking_date->format('d M Y') }}</small></span></td>
    <td data-label="Customer"><span><b>{{ $booking->customer->name }}</b><small class="block text-xs text-gray-500 dark:text-slate-400">{{ $booking->customer->phone }}</small></span></td><td data-label="Project">{{ $booking->project->name }}</td><td data-label="Package">{{ $booking->package->name }}</td><td data-label="Total">Rs {{ number_format($booking->total_price) }}</td><td data-label="Received">Rs {{ number_format($booking->paid_total??0) }}</td><td data-label="Referrer">{{ $booking->agent?->name??'Direct' }}</td>
    <td data-label="Status"><span class="rounded-full px-2 py-1 {{ $booking->status==='active'?'bg-green-100 text-green-700':($booking->status==='cancelled'?'bg-red-100 text-red-700':'bg-gray-100 text-gray-700') }}">{{ ucfirst($booking->status) }}</span></td>
    <td data-label="Actions" class="pe-4"><div class="flex w-full justify-end gap-2 sm:w-auto"><a href="{{ route('bookings.edit',$booking) }}" class="flex-1 rounded-md bg-amber-50 px-3 py-2 text-center font-semibold text-amber-700 sm:flex-none">Edit</a>@if(in_array($booking->status,['pending','approved']))<button type="button" @click="open=true" class="flex-1 rounded-md bg-slate-800 px-3 py-2 font-semibold text-white sm:flex-none">Manage</button>@else<a href="{{ route('bookings.show',$booking) }}" class="flex-1 rounded-md bg-slate-800 px-3 py-2 text-center font-semibold text-white sm:flex-none">Manage</a>@endif</div>
    @if(in_array($booking->status,['pending','approved']))<template x-teleport="body"><div x-show="open" x-cloak @keydown.escape.window="open=false" class="fixed inset-0 z-[100] flex items-center justify-center p-3 sm:p-4"><div class="absolute inset-0 bg-slate-950/70 backdrop-blur-sm" @click="open=false"></div><form method="POST" action="{{ route('bookings.status',$booking) }}" class="relative max-h-[calc(100dvh-1.5rem)] w-full max-w-lg overflow-y-auto rounded-2xl bg-white shadow-2xl dark:bg-slate-900 dark:text-white">@csrf @method('PATCH')<input type="hidden" name="status" :value="decision"><div class="sticky top-0 z-10 bg-gradient-to-r from-slate-950 to-indigo-950 p-4 text-white sm:p-5"><div class="flex justify-between gap-3"><div class="min-w-0"><div class="text-[10px] font-bold uppercase tracking-widest text-indigo-300 sm:text-xs">{{ $booking->status==='pending'?'Pending request':'Approved · awaiting first payment' }}</div><h3 class="mt-1 truncate text-lg font-black sm:text-xl">{{ $booking->booking_number }}</h3></div><button type="button" @click="open=false" class="h-9 w-9 shrink-0 rounded-full bg-white/10 text-xl">×</button></div><p class="mt-2 truncate text-sm text-slate-300">{{ $booking->customer->name }} · {{ $booking->package->name }}</p></div><div class="space-y-4 p-4 sm:p-5"><div class="grid grid-cols-1 gap-3 {{ $booking->status==='pending' ? 'sm:grid-cols-2' : '' }}">@if($booking->status==='pending')<button type="button" @click="decision='approved'" :class="decision==='approved'?'border-emerald-500 bg-emerald-50':''" class="rounded-xl border-2 p-4 text-left dark:border-slate-600"><b class="text-emerald-700">✓ Approve</b><p class="mt-1 text-xs text-slate-500">Allow first payment.</p></button>@endif<button type="button" @click="decision='cancelled'" :class="decision==='cancelled'?'border-red-500 bg-red-50':''" class="rounded-xl border-2 p-4 text-left dark:border-slate-600"><b class="text-red-700">✕ {{ $booking->status==='pending'?'Reject request':'Cancel approval' }}</b><p class="mt-1 text-xs text-slate-500">{{ $booking->status==='pending'?'Reject and release inventory.':'Cancel before the first payment is verified.' }}</p></button></div><div><div class="mb-2 flex items-center justify-between"><span class="text-xs font-black uppercase tracking-wide text-slate-500">Quick notes</span><button type="button" @click="notes=''" class="text-xs font-bold text-slate-400">Clear</button></div><div class="flex flex-wrap gap-1.5">@foreach(['Customer details and selected package have been reviewed.','Booking documents have been verified.','Customer was contacted and the booking decision was explained.','Request requires additional information before processing.'] as $note)<button type="button" @click="addNote(@js($note))" class="rounded-full bg-indigo-50 px-2.5 py-1.5 text-left text-[10px] font-bold text-indigo-700">+ {{ $note }}</button>@endforeach</div></div><label class="block text-sm font-semibold">Management notes <span x-show="decision==='cancelled'" class="text-red-600">(required)</span><textarea name="management_notes" x-model="notes" :required="decision==='cancelled'" rows="3" class="mt-1 w-full rounded-xl border-slate-300 dark:border-slate-600 dark:bg-slate-800"></textarea></label><div class="grid grid-cols-2 gap-3"><button type="button" @click="open=false" class="rounded-lg border px-4 py-2.5 font-semibold dark:border-slate-600">Close</button><button :disabled="!decision" class="rounded-lg bg-indigo-600 px-4 py-2.5 font-black text-white disabled:bg-slate-300">Save decision</button></div></div></form></div></template>@endif
    </td>
</tr>
@empty<tr><td colspan="9" class="p-10 text-center text-gray-500">No bookings found.</td></tr>@endforelse
</tbody></table></div>{{ $bookings->links() }}
</div></div></x-app-layout>
