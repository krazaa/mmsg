<x-app-layout>
<x-slot name="header"><div class="flex items-center justify-between"><h2 class="text-xl font-semibold">Manage bookings</h2><a href="{{ route('sales.create') }}" class="rounded-md bg-indigo-600 px-4 py-2 text-sm font-semibold text-white">New booking</a></div></x-slot>
<div class="py-8"><div class="mx-auto max-w-7xl space-y-5 px-4">
@if(session('success'))<div class="rounded-lg bg-green-100 p-4 text-green-800">{{ session('success') }}</div>@endif @if($errors->any())<div class="rounded-lg bg-red-100 p-4 text-red-800">{{ $errors->first() }}</div>@endif
<form method="GET" class="grid gap-4 rounded-xl bg-white p-5 shadow sm:grid-cols-4"><label class="text-sm font-medium">Search<input name="search" value="{{ request('search') }}" placeholder="Booking, customer, CNIC or phone" class="mt-1 w-full rounded-md border-gray-300"></label><label class="text-sm font-medium">Project<select name="project" class="mt-1 w-full rounded-md border-gray-300"><option value="">All projects</option>@foreach($projects as $project)<option value="{{ $project->id }}" @selected(request('project')==$project->id)>{{ $project->name }}</option>@endforeach</select></label><label class="text-sm font-medium">Status<select name="status" class="mt-1 w-full rounded-md border-gray-300"><option value="">All statuses</option>@foreach(['pending','approved','active','completed','cancelled','defaulted'] as $status)<option value="{{ $status }}" @selected(request('status')===$status)>{{ ucfirst($status) }}</option>@endforeach</select></label><div class="flex items-end gap-2"><button class="rounded-md bg-indigo-600 px-5 py-2.5 font-semibold text-white">Filter</button><a href="{{ route('bookings.index') }}" class="rounded-md border px-4 py-2.5">Clear</a></div></form>
<div class="overflow-x-auto rounded-xl bg-white shadow"><table class="w-full min-w-[1150px] text-sm"><thead class="bg-gray-50 text-left text-gray-500"><tr><th class="p-4">Booking</th><th>Customer</th><th>Project</th><th>Package</th><th>Total</th><th>Received</th><th>Referrer</th><th>Status</th><th class="pe-4 text-right">Actions</th></tr></thead><tbody>
@forelse($bookings as $booking)
<tr class="border-t" x-data="{ open:false, decision:'', notes:'', addNote(note){const current=(this.notes||'').trim();this.notes=current?current+'\n'+note:note} }">
    <td class="p-4"><b>{{ $booking->booking_number }}</b><div class="text-xs text-gray-500">{{ $booking->booking_date->format('d M Y') }}</div></td>
    <td><b>{{ $booking->customer->name }}</b><div class="text-xs text-gray-500">{{ $booking->customer->phone }}</div></td><td>{{ $booking->project->name }}</td><td>{{ $booking->package->name }}</td><td>Rs {{ number_format($booking->total_price) }}</td><td>Rs {{ number_format($booking->paid_total??0) }}</td><td>{{ $booking->agent?->name??'Direct' }}</td>
    <td><span class="rounded-full px-2 py-1 {{ $booking->status==='active'?'bg-green-100 text-green-700':($booking->status==='cancelled'?'bg-red-100 text-red-700':'bg-gray-100 text-gray-700') }}">{{ ucfirst($booking->status) }}</span></td>
    <td class="pe-4"><div class="flex justify-end gap-2"><a href="{{ route('bookings.edit',$booking) }}" class="rounded-md bg-amber-50 px-2.5 py-1.5 font-semibold text-amber-700">Edit</a>@if(in_array($booking->status,['pending','approved']))<button type="button" @click="open=true" class="rounded-md bg-slate-800 px-2.5 py-1.5 font-semibold text-white">Manage</button>@else<a href="{{ route('bookings.show',$booking) }}" class="rounded-md bg-slate-800 px-2.5 py-1.5 font-semibold text-white">Manage</a>@endif</div>
    @if(in_array($booking->status,['pending','approved']))<template x-teleport="body"><div x-show="open" x-cloak @keydown.escape.window="open=false" class="fixed inset-0 z-[100] flex items-center justify-center p-4"><div class="absolute inset-0 bg-slate-950/70 backdrop-blur-sm" @click="open=false"></div><form method="POST" action="{{ route('bookings.status',$booking) }}" class="relative w-full max-w-lg overflow-hidden rounded-2xl bg-white shadow-2xl">@csrf @method('PATCH')<input type="hidden" name="status" :value="decision"><div class="bg-gradient-to-r from-slate-950 to-indigo-950 p-5 text-white"><div class="flex justify-between"><div><div class="text-xs font-bold uppercase tracking-widest text-indigo-300">{{ $booking->status==='pending'?'Pending request':'Approved · awaiting first payment' }}</div><h3 class="mt-1 text-xl font-black">{{ $booking->booking_number }}</h3></div><button type="button" @click="open=false" class="h-9 w-9 rounded-full bg-white/10 text-xl">×</button></div><p class="mt-2 text-sm text-slate-300">{{ $booking->customer->name }} · {{ $booking->package->name }}</p></div><div class="space-y-4 p-5"><div class="grid {{ $booking->status==='pending'?'grid-cols-2':'grid-cols-1' }} gap-3">@if($booking->status==='pending')<button type="button" @click="decision='approved'" :class="decision==='approved'?'border-emerald-500 bg-emerald-50':''" class="rounded-xl border-2 p-4 text-left"><b class="text-emerald-700">✓ Approve</b><p class="mt-1 text-xs text-slate-500">Allow first payment.</p></button>@endif<button type="button" @click="decision='cancelled'" :class="decision==='cancelled'?'border-red-500 bg-red-50':''" class="rounded-xl border-2 p-4 text-left"><b class="text-red-700">✕ {{ $booking->status==='pending'?'Reject request':'Cancel approval' }}</b><p class="mt-1 text-xs text-slate-500">{{ $booking->status==='pending'?'Reject and release inventory.':'Cancel before the first payment is verified.' }}</p></button></div><div><div class="mb-2 flex items-center justify-between"><span class="text-xs font-black uppercase tracking-wide text-slate-500">Quick notes</span><button type="button" @click="notes=''" class="text-xs font-bold text-slate-400">Clear</button></div><div class="flex flex-wrap gap-1.5">@foreach(['Customer details and selected package have been reviewed.','Booking documents have been verified.','Customer was contacted and the booking decision was explained.','Request requires additional information before processing.'] as $note)<button type="button" @click="addNote(@js($note))" class="rounded-full bg-indigo-50 px-2.5 py-1.5 text-left text-[10px] font-bold text-indigo-700">+ {{ $note }}</button>@endforeach</div></div><label class="block text-sm font-semibold">Management notes <span x-show="decision==='cancelled'" class="text-red-600">(required)</span><textarea name="management_notes" x-model="notes" :required="decision==='cancelled'" rows="3" class="mt-1 w-full rounded-xl border-slate-300"></textarea></label><div class="flex justify-end gap-3"><button type="button" @click="open=false" class="rounded-lg border px-4 py-2 font-semibold">Close</button><button :disabled="!decision" class="rounded-lg bg-indigo-600 px-5 py-2 font-black text-white disabled:bg-slate-300">Save decision</button></div></div></form></div></template>@endif
    </td>
</tr>
@empty<tr><td colspan="9" class="p-10 text-center text-gray-500">No bookings found.</td></tr>@endforelse
</tbody></table></div>{{ $bookings->links() }}
</div></div></x-app-layout>
